# Les variables en Rust 🦀

Aujourd'hui, on va parler des variables.  
Tu sais, ces boîtes où tu ranges des infos, comme en JS ou Python.

## Déclarer une variable

```rust
let nom = "Rusty";
```

Ici, on crée une variable `nom` qui contient… ben, "Rusty" !

> **Astuce de Rusty**  
> Par défaut, une variable ne peut pas changer de valeur.  
> Si tu veux la rendre "modulable", ajoute `mut` :

```rust
let mut score = 0;
score = 42;
```

## Parallèle Web

En JS, tu fais :

```js
let nom = "Rusty";
nom = "Crabzilla";
```

En Rust, c'est pareil, mais il faut le dire explicitement si tu veux changer la valeur.

---

## QCM de Rusty



---

*On continue ? Clique sur le chapitre suivant pour découvrir les types de données !* 